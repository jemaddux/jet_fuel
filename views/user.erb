<!DOCTYPE HTML>
<html>
<head><title>JetFuel - User</title>
  
</head>
<body>
  <h1><a href="http://jetfuel.herokuapp.com">JetFuel</a></h1>

  <h2>Hi User: <%= @user.name %></h2>

  <form method="POST" action="/user_shorten/<%= @user.name %>"> <p>Shorten your link:</p>
    <label>Url:</label>
    <input type="text" name="url" />
    <label>Vanity:</label>
    <input type="text" name="vanity" />
    <input type="submit" name="shorten" value="Shorten" id="shorten_button"/>
  </form>

  <ul>
    <% @user_links.each do |link|%>
      <li><%= link.full_url %>
        was shortened to 
        <a href="http://<%= link.short_url %>"><%= link.short_url %></a>
      </li>
    <% end %>
  </ul>

</body>
</html>
