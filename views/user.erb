<!DOCTYPE HTML>
<html>
<head><title>JetFuel - User</title>
  <style>
    body{
      padding: 0px;
      margin: 0px;
    }
    .top{
      width: 100%;
      background-color: orange;
      margin-top: 0px;
      padding: 15px;
      border-bottom: 0px solid black;
    }
    .top_stuff{
      color: black;
      font-family: serif;
      display: inline;
      margin: 0px;
      padding: 4px;
      position: absolute;
      top: 20px;
      right: 10px;
      width: 200px;
    }
    .top_stuff label{
      font-size: 120%;
    }
    .top_stuff input{
      width: 100px;
    }
    h1{
      margin: 0px;
      padding: 0px;
    }
    h1 a{
      color: black;
      font-family: serif;
      font-size: 120%;
    }
    #main{
      width: 80%;
      margin: auto;
    }
  </style>
  <script src="//ajax.googleapis.com/ajax/libs/jquery/1.9.1/jquery.min.js">
  </script>
</head>
<body>
  <div class="top">
    <h1><a href="http://jetfuel.herokuapp.com">JetFuel</a></h1>
    <h2 class="top_stuff">Hi User: <%= @user.name %></h2>
  </div>

  <div id="main">
    <form method="POST" action="/user_shorten/<%= @user.name %>"> <p>Shorten your link:</p>
      <label>Url:</label>
      <input type="text" name="url" />
      <label>Vanity:</label>
      <input type="text" name="vanity" />
      <input type="submit" name="shorten" value="Shorten" id="shorten_button"/>
    </form>

    <div class="table">
      <h2>User Links</h2>
        <table>
        <% @user_links.each do |user_link| %>
          <tr>
            <td><%= user_link.full_url %></td>
            <td>Viewed <%= user_link.visits_count %> times</td> 
            <td>
              Via: <a href="http://<%= user_link.short_url %>">
              <%= user_link.short_url %></a>
            </td>
          </tr>
        <% end %>
        </table>
    </div>

  </div>

</body>
<script type="text/javascript">
  $(document).ready(function() {
      var randClr = '#'+Math.floor(Math.random()*16777215).toString(16);
      $('.top').css("background-color", randClr);
      $('.top').click(function(){
        var randClr = '#'+Math.floor(Math.random()*16777215).toString(16);
      $('.top').css("background-color", randClr);
      });
  });
</script>
<script type="text/javascript">

  var _gaq = _gaq || [];
  _gaq.push(['_setAccount', 'UA-39299522-1']);
  _gaq.push(['_trackPageview']);

  (function() {
    var ga = document.createElement('script'); ga.type = 'text/javascript'; ga.async = true;
    ga.src = ('https:' == document.location.protocol ? 'https://ssl' : 'http://www') + '.google-analytics.com/ga.js';
    var s = document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(ga, s);
  })();

</script>
</html>
